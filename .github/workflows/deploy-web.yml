name: Deploy Frontend

on:
  push:
    branches: [ main ]
    paths: [ "apps/web/**" ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "pnpm"
          cache-dependency-path: "apps/web/pnpm-lock.yaml"

      - name: Install dependencies
        run: cd apps/web && pnpm install

      - name: Build
        run: cd apps/web && pnpm build

      # These steps are commented out until AWS credentials are set up
      # - name: Deploy to S3
      #   run: aws s3 sync apps/web/dist s3://${{ secrets.AWS_S3_BUCKET }} --delete
      
      # - name: Invalidate CloudFront
      #   run: aws cloudfront create-invalidation --distribution-id ${{ secrets.AWS_CLOUDFRONT_DIST_ID }} --paths "/*"
